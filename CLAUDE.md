# CLAUDE.md

Read @AGENTS.md before changing anything in this repo.

Note that this file is only a pointer for agents working on the repo. It is not the user's global instruction file. Those rules live in `rules/global.md`, which `install.sh` symlinks to `~/.claude/CLAUDE.md`.
